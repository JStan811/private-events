<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <a href="/users/<%= user.id %>">
        <%= user.username %>
      </a>
    </li>
  <% end %>
</ul>

<%= button_to "back to events page", events_path, method: :get  %>
